# Kubernetes secrets (plaintext)
*-secret-temp.yaml
*-temp.yaml
secret-temp.yaml

# Sealed Secrets backup keys (NEVER commit these!)
sealed-secrets-key-backup.yaml
*-key-backup.yaml

# Kubeconfig files with credentials
*.kubeconfig
kubeconfig

# Helm values with secrets (but not sealed-secrets app)
*-secrets.yaml
!argocd-apps/sealed-secrets.yaml

# Recyclarr secrets (never commit the actual secret file)
apps/media-server/recyclarr/recyclarr-secret.yaml

# AdGuard Home sync credentials (plaintext password)
apps/adguard-home/sync-secret.yaml

# Acestream (managed manually, not via GitOps)
apps/acestream/
argocd-apps/acestream.yaml

# Backups (keep local only)
backups/

# Local development
.env
.env.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
nohup.out

apps/adguard-home/sync-secret.yaml

# Claude Code (local only)
CLAUDE.md
tasks/
