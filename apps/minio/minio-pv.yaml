apiVersion: v1
kind: PersistentVolume
metadata:
  name: minio-pv
  labels:
    app: minio  # This label ensures the Helm chart can bind the PV
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  nfs:
    path: /volume1/minio-backup
    server: 192.168.1.42

