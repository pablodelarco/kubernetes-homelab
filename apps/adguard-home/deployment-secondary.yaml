# AdGuard Home - Secondary instance (worker)
# Provides DNS redundancy, config is synced from primary
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adguard-home-secondary
  namespace: adguard-home
  labels:
    app: adguard-home
    adguard-role: secondary
spec:
  replicas: 1
  # Recreate strategy required for hostNetwork (port 53 conflict)
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: adguard-home
      adguard-role: secondary
  template:
    metadata:
      labels:
        app: adguard-home
        adguard-role: secondary
    spec:
      # Use host network so DNS is accessible on node IP
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      # Pin to worker node
      nodeSelector:
        kubernetes.io/hostname: worker
      initContainers:
        # Copy initial config if not exists
        - name: init-config
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              if [ ! -f /opt/adguardhome/conf/AdGuardHome.yaml ]; then
                echo "Copying initial configuration..."
                cp /config/AdGuardHome.yaml /opt/adguardhome/conf/
              else
                echo "Configuration already exists, skipping..."
              fi
          volumeMounts:
            - name: config-volume
              mountPath: /config
            - name: conf
              mountPath: /opt/adguardhome/conf
      containers:
        - name: adguard-home
          image: adguard/adguardhome:v0.107.54
          ports:
            - name: dns-tcp
              containerPort: 53
              protocol: TCP
            - name: dns-udp
              containerPort: 53
              protocol: UDP
            - name: http
              containerPort: 3000
              protocol: TCP
          volumeMounts:
            - name: work
              mountPath: /opt/adguardhome/work
            - name: conf
              mountPath: /opt/adguardhome/conf
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config-volume
          configMap:
            name: adguard-home-config
        - name: work
          persistentVolumeClaim:
            claimName: adguard-home-secondary-work
        - name: conf
          persistentVolumeClaim:
            claimName: adguard-home-secondary-conf

