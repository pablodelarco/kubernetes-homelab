apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-pvc-backup
  namespace: home-assistant
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
