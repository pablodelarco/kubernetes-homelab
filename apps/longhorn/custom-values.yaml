persistence:
  defaultClass: true
defaultSettings:
  backupTarget: "s3://longhorn-backups@us-east-1/"
  backupTargetCredentialSecret: "minio-credentials"
service:
  ui:
    type: LoadBalancer
    port: 80
