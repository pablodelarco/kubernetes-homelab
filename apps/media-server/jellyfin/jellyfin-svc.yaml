# ClusterIP service for Jellyfin - access via Cilium Gateway
apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  namespace: media
  labels:
    app: jellyfin
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8096
      targetPort: 8096
  selector:
    app: jellyfin
