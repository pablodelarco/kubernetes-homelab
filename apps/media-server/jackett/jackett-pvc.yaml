apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  volumeName: pvc-jackett-restored
  resources:
    requests:
      storage: 5Gi